{
    "name": "homebridge-tuya-osoji-vacuum",
    "version": "1.0.7",
    "description": "Plugin para controlar la aspiradora Osoji X420 mediante Tuya Cloud API.",
    "license": "ISC",
    "main": "dist/index.js",
    "engines": {
        "node": ">=18.0.0",
        "homebridge": ">=1.8.0"
    },
    "scripts": {
        "build": "rimraf ./dist && tsc",
        "watch": "rimraf ./dist && tsc -w",
        "prepublishOnly": "npm run build"
    },
    "keywords": [
        "homebridge-plugin",
        "tuya",
        "osoji",
        "vacuum",
        "cleaner"
    ],
    "dependencies": {
        "@tuya/tuya-connector-nodejs": "^2.1.2"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "homebridge": "^1.8.0",
        "rimraf": "^5.0.0",
        "typescript": "^5.0.0"
    }
}
